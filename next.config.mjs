/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "d12zq4w4guyljn.cloudfront.net",
      },
    ],
  },
};

export default nextConfig;
